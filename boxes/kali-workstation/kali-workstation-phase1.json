{
    "variables": {
        "boot_wait": "15s",
        "communicator": "ssh",
        "cpus": "4",
        "headless": "false",
        "memory": "4096",
        "shutdown_command": "sudo systemctl poweroff -i",
        "ssh_private_key_file": "../../vagrant.id_rsa",
        "ssh_timeout": "10m",
        "ssh_username": "vagrant",
        "vm_name": "kali-workstation"
    },

    "builders": [
        {
            "type": "vmware-vmx",

            "display_name": "kali-workstation",
            "source_path": "../kali-base/output-vmware-iso/kali-base.vmx",

            "boot_wait": "{{ user `boot_wait` }}",
            "communicator": "{{ user `communicator` }}",
            "headless": "{{ user `headless` }}",
            "shutdown_command": "{{ user `shutdown_command` }}",
            "ssh_private_key_file": "{{ user `ssh_private_key_file` }}",
            "ssh_timeout": "{{ user `ssh_timeout` }}",
            "ssh_username": "{{ user `ssh_username` }}",
            "vm_name": "{{ user `vm_name` }}"
        },
        {
            "type": "virtualbox-ovf",

            "source_path": "../kali-base/output-virtualbox-iso/kali-base.ovf",

            "boot_wait": "{{ user `boot_wait` }}",
            "communicator": "{{ user `communicator` }}",
            "headless": "{{ user `headless` }}",
            "shutdown_command": "{{ user `shutdown_command` }}",
            "ssh_private_key_file": "{{ user `ssh_private_key_file` }}",
            "ssh_timeout": "{{ user `ssh_timeout` }}",
            "ssh_username": "{{ user `ssh_username` }}",
            "vm_name": "{{ user `vm_name` }}"
        },
        {
            "type": "hyperv-vmcx",

            "clone_from_vmcx_path": "../kali-base/output-hyperv-iso",
            "cpus": "{{ user `cpus` }}",
            "generation": 2,
            "memory": "{{ user `memory` }}",
            "output_directory": "output-hyperv-vmcx-phase1",
            "switch_name": "Default Switch",

            "boot_wait": "{{ user `boot_wait` }}",
            "communicator": "{{ user `communicator` }}",
            "headless": "{{ user `headless` }}",
            "shutdown_command": "{{ user `shutdown_command` }}",
            "ssh_private_key_file": "{{ user `ssh_private_key_file` }}",
            "ssh_timeout": "{{ user `ssh_timeout` }}",
            "ssh_username": "{{ user `ssh_username` }}",
            "vm_name": "{{ user `vm_name` }}-phase1"
        }
    ],

    "provisioners": [
        {
            "type": "shell",
            "execute_command": "sudo bash '{{ .Path }}'",
            "script": "scripts/setup.sh"
        },
        {
            "type": "shell",
            "script": "scripts/setup-user.sh"
        },
        {
            "type": "shell",
            "only": "vmware-vmx",
            "execute_command": "sudo bash '{{ .Path }}'",
            "script": "scripts/setup-vmware.sh"
        },
        {
            "type": "shell-local",
            "only": [ "vmware-vmx", "virtualbox-ovf" ],
            "inline": [
                "powershell.exe Set-VM -VMName {{ user `vm_name` }} -EnhancedSessionTransportType HvSocket"
            ]
        }
    ],

    "post-processors": [
        {
            "type": "vagrant",
            "only": [ "vmware-vmx", "virtualbox-ovf" ],
            "keep_input_artifact": true,
            "output": "{{ user `vm_name` }}-{{ .Provider }}.box"
        },
        {
            "type": "shell-local",
            "inline": [
                "ssh-keygen -R {{ build `Host` }}"
            ]
        }
    ]
}