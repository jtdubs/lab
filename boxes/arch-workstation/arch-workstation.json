{
    "builders": [
        {
            "type": "vmware-vmx",
            "source_path": "../arch-base/output-vmware-iso/arch-base.vmx",
            "shutdown_command": "sudo systemctl poweroff -i",
            "communicator": "ssh",
            "ssh_username": "packer",
            "ssh_private_key_file": "../../packer.id_rsa",
            "ssh_timeout": "10m",
            "boot_wait": "15s",
            "vm_name": "arch-workstation",
            "display_name": "arch-workstation"
        },
        {
            "type": "virtualbox-ovf",
            "source_path": "../arch-base/output-virtualbox-iso/arch-base.ovf",
            "shutdown_command": "sudo systemctl poweroff -i",
            "communicator": "ssh",
            "ssh_username": "packer",
            "ssh_private_key_file": "../../packer.id_rsa",
            "ssh_timeout": "10m",
            "boot_wait": "15s",
            "vm_name": "arch-workstation"
        }
    ],

    "provisioners": [
        {
            "type": "shell",
            "script": "scripts/setup.sh"
        },
        {
            "type": "shell",
            "execute_command": "sudo -u jtdubs bash '{{ .Path }}'",
            "script": "scripts/setup-jtdubs.sh"
        }
    ],

    "post-processors": [
        {
            "type": "shell-local",
            "inline": [
                "ssh-keygen -R {{ build `Host` }}"
            ]
        }
    ]
}
