arch-workstation;
    clone dotfiles and install them as jtdubs

general:
    ansible: use ssh keys rather than id/pw?
    use ansible-vault for ssh keys?
    use ansible-vault for jtdubs password
    fix known_hosts file issue
