- Ubuntu base image
- Need a vagrant user and ssh key on the box (replace packer w/ vagrant??)
- Split packer into VM builds & Vagrant packager files

structure:
    boxes/
        arch-base/
            ...
        arch-workstation/
            ...
    envs/
        desktop/
            Vagrantfile
        lab/
            Vagrantfile
